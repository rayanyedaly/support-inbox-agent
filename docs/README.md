# docs

Screenshots referenced by the root `README.md` — add two captures here:

- `screenshot-light.png` — the app in light mode (the ticket detail view shows the
  most: sidebar, customer thread, the staged draft card, and the agent-run trace panel).
- `screenshot-dark.png` — the same view with the dark theme.

Capture at ~1480px wide against a seeded DB: `npm run dev`, open a ticket, and use the
Light/Dark toggle at the bottom of the sidebar.
